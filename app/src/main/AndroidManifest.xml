<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cs4310.epsilon.buynutsproto" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".activities.MainLoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.NewsActivity" >
        </activity>
        <activity android:name=".activities.MakeOfferActivity" >
        </activity>
        <activity android:name=".activities.SetSearchFilterActivity" >
        </activity>
        <activity android:name=".activities.ViewSellOfferActivity" >
        </activity>
        <activity android:name=".activities.ContactSellerActivity" >
        </activity>
    </application>

</manifest>
